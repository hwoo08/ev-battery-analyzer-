# 자원순환 경로 판정 정책

> ⚠️ **먼저 밝혀둘 것**
> 이 문서의 임계값·단가·데이터는 **특정 기업의 내부 판정 기준이 아닙니다.**
> 공개된 보도자료와 산업 자료를 참고해 이 프로젝트를 위해 설계한 모델이며,
> 실무에 그대로 적용할 수 있는 기준이 아닙니다. 어디까지가 참고한 사실이고
> 어디부터가 제 설계인지 아래에 구분해 적었습니다.

## 1. 왜 점수가 아니라 '경로'인가

기존 [평가 모델](evaluation_method.md)은 배터리 상태를 100점으로 환산했습니다.
하지만 사용 후 배터리 산업에서 실제로 내려야 하는 결정은 점수가 아니라
**"이 배터리를 어디로 보낼 것인가"** 입니다.

전자신문 보도([2024-08-22](https://www.etnews.com/20240822000195))에 따르면
사용 후 배터리 산업은 다음 세 갈래로 구분됩니다.

| 경로 | 내용 |
|---|---|
| **재제조** (Remanufacture) | 분해·검사·재조립을 거쳐 다시 전기차 배터리로 사용 |
| **재사용** (Reuse) | ESS 등으로 용도를 바꿔 사용 |
| **재활용** (Recycle) | 분해해 원료(리튬·니켈·코발트 등)를 추출 |

같은 기사에서 포엔의 공정은 이렇게 소개됩니다.

> 완성차 업체에서 사용 후 배터리를 수거 → 분해 후 성능 검사 →
> **안전 기준에 따른 평가** → **잔존 수명 80% 이상 선별** → 재조립 →
> 전기차 배터리로 재활용

이 프로젝트는 위 흐름을 코드로 옮긴 것입니다.

## 2. 판정 순서

```
입고 배터리
    │
    ├─▶ [1] 안전성 게이트 ─── 미달 ──▶ 재활용
    │        (SOH와 무관하게 우선 적용)
    ▼
   [2] SOH 1차 분류
    │
    ├── 80% 이상 ──▶ [3] 재제조 정밀 조건 ──┬─ 충족 ──▶ 재제조
    │                                      └─ 미충족 ─▶ 재사용 (강등)
    ├── 60 ~ 80% ─────────────────────────────────────▶ 재사용
    └── 60% 미만 ─────────────────────────────────────▶ 재활용
```

**안전성을 SOH보다 먼저 보는 이유**: 재제조와 재사용은 배터리를 계속
'쓰는' 경로입니다. 잔존 용량이 아무리 많아도 셀 밸런싱이 무너졌거나 열
이력이 심하거나 BMS가 반복적으로 이상을 감지한 팩을 다시 쓰는 것은
위험합니다. 그래서 안전성을 통과 조건(gate)으로 두고, 통과한 팩에 대해서만
잔존 용량을 따집니다.

## 3. 임계값

### 3.1 안전성 게이트 — 하나라도 초과하면 재활용

| 항목 | 한계 | 근거 |
|---|---|---|
| 셀 전압 편차 | 100 mV 초과 | ⚠️ **자체 설계.** 셀 간 불균형이 크면 밸런싱으로 회복이 어렵고 과충전·과방전 위험이 커진다는 일반적 원리를 반영 |
| 최고 온도 이력 | 60℃ 초과 | ⚠️ **자체 설계.** 고온 노출이 열화를 가속하고 열폭주 위험과 연결된다는 일반적 원리를 반영 |
| BMS 오류 누적 | 5회 초과 | ⚠️ **자체 설계.** 반복적인 오류는 셀·센서·보호회로 이상 가능성을 시사한다고 보고 설정 |

### 3.2 SOH 1차 분류

| 구간 | 경로 | 근거 |
|---|---|---|
| 80% 이상 | 재제조 후보 | ✅ **참고한 사실.** 포엔 관련 보도에서 "잔존 수명 80% 이상 선별"로 언급 |
| 60 ~ 80% | 재사용 (ESS) | ⚠️ **자체 설계.** 전기차 주행에는 부족하지만 정치형 ESS로는 활용 가능하다고 보고 설정한 구간 |
| 60% 미만 | 재활용 | ⚠️ **자체 설계.** 용도 전환보다 원료 회수가 합리적이라고 본 하한 |

### 3.3 재제조 정밀 조건 — 미충족 시 재사용으로 강등

전기차에 다시 들어가는 만큼 안전성 게이트보다 엄격하게 재검합니다.

| 항목 | 기준 |
|---|---|
| 셀 전압 편차 | 50 mV 이하 |
| 최고 온도 이력 | 50℃ 이하 |
| BMS 오류 | 0회 |
| 급속충전 비율 | 60% 이하 |

⚠️ 네 항목 모두 **자체 설계**입니다.

## 4. 경제성 추정

경로별 예상 잔존가치를 이렇게 계산합니다.

```
재제조·재사용 = 팩 용량(kWh) × (SOH / 100) × 경로별 단가
재활용        = 팩 용량(kWh) × 재활용 단가
```

재제조·재사용은 '남아 있는 용량'을 쓰는 것이므로 SOH를 곱하고,
재활용은 원료를 회수하는 것이라 잔존 용량과 무관하게 팩 용량을 기준으로 봅니다.

| 경로 | 가정 단가 |
|---|---|
| 재제조 | 60,000 원/kWh |
| 재사용 | 25,000 원/kWh |
| 재활용 | 8,000 원/kWh |

⚠️ **세 단가 모두 가정치입니다.** 신뢰할 만한 공개 시세를 확보하지 못해,
경로 간 상대적 크기(재제조 > 재사용 > 재활용)를 보여주기 위한 값으로 두었습니다.
실제 거래가와 다르며, **금액의 절대값에는 의미를 두면 안 됩니다.**
실 데이터를 구하면 `routing.py`의 `VALUE_ASSUMPTIONS_KRW_PER_KWH` 한 곳만
바꾸면 됩니다.

## 5. 데이터

`data/incoming_batteries.csv`의 120대는 **합성 데이터**입니다.
실제 차량에서 수집한 값이 아니라 `tools/generate_sample_data.py`가
아래 경향을 반영해 생성한 값이며, 난수 시드를 고정해 누구나 같은 결과를
재현할 수 있습니다.

- 사용기간·주행거리가 늘수록 SOH가 낮아진다
- 급속충전 비율이 높을수록 SOH가 조금 더 낮아진다
- SOH가 낮을수록 셀 전압 편차와 BMS 오류가 커지는 경향이 있다
- 일부 팩은 온도 이상·셀 밸런싱 이상·BMS 다발 오류를 가진다

생성 규칙 전체는 해당 스크립트에 주석과 함께 공개돼 있습니다.

## 6. 이 시스템이 보여주려는 것

판정 결과에서 가장 눈여겨볼 부분은 **SOH 80% 이상인데도 재활용으로 분류된
배터리**입니다. 잔존 용량만 봤다면 전기차에 다시 들어갔을 팩이지만,
셀 전압 편차·온도 이력·BMS 오류에서 걸러진 경우입니다.

SOH 하나로 판단하는 것과 안전성을 함께 보는 것의 차이가 실제 숫자로
드러나는 지점이며, 이 프로젝트를 단순 계산기와 구분 짓는 부분입니다.

## 7. 한계와 다음 단계

| 한계 | 다음 단계 |
|---|---|
| 임계값 대부분이 실측 검증 없이 설계된 값 | 공개 배터리 열화 데이터셋으로 임계값을 보정 |
| 데이터가 합성 데이터 | 실측 BMS 로그 확보 |
| SOH를 입력값으로 받음 (추정하지 않음) | 전압·전류·온도 시계열로 SOH를 직접 추정 |
| 잔존수명을 예측하지 않음 | RUL(잔존수명) 예측 모델 추가 |
| 단가가 가정치 | 실제 거래 데이터 기반으로 교체 |

## 8. 구현 메모

같은 판정 로직을 두 곳에 구현했습니다.

- `routing.py` — 터미널 버전
- `assets/routing.js` — 웹 대시보드 버전

한쪽만 고치면 두 화면의 판정이 달라지므로, 파라미터를 바꾼 뒤에는
반드시 아래를 실행해 120대 전 항목이 일치하는지 확인합니다.

```bash
python3 tools/verify_engines.py
```

## 참고 자료

- [\[人사이트\] 최성진 포엔 대표 "전기차 배터리 재제조 생태계 구축 도전" — 전자신문](https://www.etnews.com/20240822000195)
- [배터리 재사용·재활용 산업 활성화 방향 — 한국자동차연구원](https://www.katech.re.kr/download/550c6265-2a35-40ef-a0be-0e585611be5a)
- [전기차 폐배터리의 가치를 높이는 재활용 솔루션, UBESS — 현대자동차그룹](https://www.hyundaimotorgroup.com/ko/story/CONT0000000000001676)
- [사용 후 배터리 모듈 잔존 가치 평가 시스템 및 그 방법 (WO2022158774A1)](https://patents.google.com/patent/WO2022158774A1/ko)
